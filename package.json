{
  "name": "auto-aws-sso-creds",
  "projectName": "auto-aws-sso-creds",
  "description": "script to (almost)auto update aws sso credentials",
  "version": "1.0.4",
  "exports": "./dist/app.js",
  "types": "./dist/app.d.ts",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rafaelndev/aws-sso-creds-tool.git"
  },
  "scripts": {
    "build": "tsc",
    "start": "node ./dist/app.js",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "dist",
    "src"
  ],
  "homepage": "https://github.com/leandrosalo/aws-sso-creds-tool#readme",
  "dependencies": {
    "@aws-sdk/client-sso": "^3.18.0",
    "@aws-sdk/client-sso-oidc": "^3.18.0",
    "chalk": "^5.6.2",
    "confbox": "^0.2.2",
    "open": "^10"
  },
  "keywords": [
    "aws",
    "sso",
    "cloud",
    "credentials",
    "aws sso credentials",
    "aws authentication",
    "aws single sign on",
    "single sign on"
  ],
  "bin": {
    "auto-aws": "./dist/app.js"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=18"
  },
  "author": "",
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
